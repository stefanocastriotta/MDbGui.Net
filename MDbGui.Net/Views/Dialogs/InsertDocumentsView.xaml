<Window x:Class="MDbGui.Net.Views.Dialogs.InsertDocumentsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ignore="http://www.galasoft.ch/ignore"
        mc:Ignorable="d ignore">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Skins/MainSkin.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
        </Grid.RowDefinitions>
        <Label Content="Insert documents in JSON array [{...},{...}]"></Label>
        <TextBox Text="{Binding Documents}" AcceptsReturn="True" TextWrapping="Wrap" MinHeight="500" Grid.Row="1" Style="{StaticResource ResultsView}">
            
        </TextBox>
        <StackPanel Grid.Row="2" Orientation="Horizontal">
            <Button Content="Insert" Command="{Binding Insert}"></Button>
            <Button Content="Close" Click="Button_Click"></Button>
        </StackPanel>
    </Grid>
</Window>
